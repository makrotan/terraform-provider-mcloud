up:
  commands:
    - cmd: make install
    # terraform
    - cmd: rm -rf examples/.terraform.lock.hcl
    - cmd: terraform init
      working_dir: examples
    - cmd: terraform apply -auto-approve
      working_dir: examples
#    - cmd: terraform plan
#      working_dir: examples

